---
sudo: false
language: ruby
#cache: bundler
rvm:
  - 2.2.4
before_install: 
  - gem install bundler -v '1.17'
  - gem install bundler -v '~> 1.17'
install: 
  - bundle install
script:
  - sudo find / -name "studio.*"
  - ls
  - rspec spec/tests/selection_tool_spec.rb
#  - bundle exec rake 
#  - bundle exec rake openstudio:test_with_openstudio
#  - bundle exec rake   